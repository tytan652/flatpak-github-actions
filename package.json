{
  "name": "flatpak-github-actions",
  "private": true,
  "main": "dist/add-remotes/index.js",
  "license": "MIT",
  "scripts": {
    "build": "tsc && ncc build lib/add-remotes.js -o dist/add-remotes && ncc build lib/build-full.js -o dist/build-full"
  },
  "dependencies": {
    "@actions/core": "^2.0.3",
    "@actions/exec": "^2.0.0"
  },
  "devDependencies": {
    "@types/node": "^25.3.3",
    "@vercel/ncc": "^0.38.4",
    "typescript": "^5.9.3"
  }
}
